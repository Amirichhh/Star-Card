import os

# =========================
#   ОСНОВНЫЕ НАСТРОЙКИ
# =========================

BOT_TOKEN = os.getenv("BOT_TOKEN", "8977236753:AAE_lEIxkj00CDwO_P536kvSA9vEYWlcOZ0")

# ID главного администратора (главных админов может быть несколько)
ADMIN_IDS = [1727103213]  # <-- замени на свой Telegram ID

DB_PATH = os.path.join(os.path.dirname(__file__), "data", "star_card.db")
CARDS_DIR = os.path.join(os.path.dirname(__file__), "data", "rendered_cards")

# =========================
#   ЭКОНОМИКА
# =========================

# Комиссия биржи (при продаже/покупке карты между пользователями), в долях (0.07 = 7%)
MARKET_COMMISSION = 0.07

# Стоимость ПОПЫТКИ улучшения (гача-пул), в звёздах.
# В первый час после запуска улучшения - дороже, затем цена линейно падает
# ступенями раз в 10 минут, пока не достигнет базовой цены.
UPGRADE_LAUNCH_PRICE = 50
UPGRADE_BASE_PRICE = 15
UPGRADE_DECAY_MINUTES = 60
UPGRADE_DECAY_STEP_MINUTES = 10

# Шансы выпадения редкости при улучшении (сумма = 1.0)
RARITY_DROP_RATES = {
    "rare": 0.60,
    "epic": 0.25,
    "mythic": 0.12,
    "legendary": 0.03,
}

# Множители ЦЕННОСТИ улучшенной карты относительно текущего курса базовой карты.
# Ценность улучшенной карты = current_rate(база) * множитель - то есть растёт
# и падает вместе с курсом базовой карты.
RARITY_MULTIPLIERS = {
    "rare": 1.15,
    "epic": 1.35,
    "mythic": 1.70,
    "legendary": 2.50,
}

RARITY_NAMES = {
    "rare": "🔵 Редкая",
    "epic": "🟣 Эпическая",
    "mythic": "🟠 Мифическая",
    "legendary": "🟡 Легендарная",
}

RARITY_ORDER = ["rare", "epic", "mythic", "legendary"]

# Параметры алгоритма курса (по типу биржевого, EMA + давление спроса/предложения)
RATE_ALPHA = 0.25          # вес нового трейда в среднем курсе
RATE_BUY_PRESSURE = 0.02   # доп. рост курса при покупке
RATE_SELL_PRESSURE = 0.02  # доп. падение курса при продаже
RATE_MIN_FACTOR = 0.30     # курс не может упасть ниже 30% от базовой цены

# Шаг падения курса при автопродаже одной карты боту (аналог проскальзывания/slippage)
AUTO_SELL_IMPACT = 0.02    # -2% за каждую проданную единицу в одной пачке

# Крафт: конкретные карты-ингредиенты (любого типа, включая улучшенные) ->
# 1 карта-результат. Бесплатный (звёзд не берётся), но ингредиенты сгорают
# независимо от результата - шанс успеха задаёт админ для каждого рецепта.
# Бонус ценности карты-результата относительно суммы вложенных ингредиентов
# (наценка на крафточные карты).
CRAFT_VALUE_BONUS = 2.2

# Ключ в settings: показывать ли кнопку «🧪 Крафт» в главном меню пользователя.
# Управляется админом из панели администратора в любой момент.
CRAFT_MENU_SETTING_KEY = "craft_menu_visible"

# Минимальная сумма пополнения / вывода звёзд
MIN_TOPUP = 1
MIN_WITHDRAW = 15

# Комиссия при переводе звёзд между пользователями (0.05 = 5%)
TRANSFER_COMMISSION = 0.05
MIN_TRANSFER = 1

# Создание карты обычным пользователем: стартовая цена всегда такая,
# плюс комиссия, которая списывается ПОСЛЕ одобрения карты модератором/админом.
CUSTOM_CARD_START_PRICE = 10
CUSTOM_CARD_APPROVAL_FEE = 100

# =========================
#   ПРОЧЕЕ
# =========================

SUPPORT_CONTINUE_HINT = (
    "\n\n✉️ Просто напишите сообщение сюда, чтобы продолжить диалог по этому обращению."
)

# Ключ в таблице settings, где хранится ID канала для авто-постинга об одобренных
# выводах звёзд. Настраивается командой /set_withdraw_channel в панели админа.
WITHDRAW_LOG_CHANNEL_SETTING_KEY = "withdraw_log_channel_id"
